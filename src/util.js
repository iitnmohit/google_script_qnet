function copylinkfromnametofbtab() {
  //var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName("NAME LIST");
 // var range = sheet.getRange(2, 3, 1061, 1);//1061
  //var formulas = range.getRichTextValues();
  //for(var row = 0;row < formulas.length;row++){
  //  if(formulas[row][0].getLinkUrl() != null){
  ///   var richText = SpreadsheetApp.newRichTextValue()
     //                              .setText("FACEBOOK")
       //                            .setLinkUrl(formulas[row][0].getLinkUrl())
         //                          .build();
//     sheet.getRange(2 + row, 18).setRichTextValue(richText);
  //   
    // sheet.getRange(2 + row, 3).clear({contentsOnly: true});
     //sheet.getRange(2 + row, 3).setValue(formulas[row][0].getText());
      
    //}
  //}
}
